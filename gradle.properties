android.useAndroidX=true
android.enableJetifier=true
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# CVE-2026-53914 is in KAPT's incremental cache deserialization. KAPT is not
# used by this project; keep its vulnerable cache path disabled if it is added.
kapt.incremental.apt=false
